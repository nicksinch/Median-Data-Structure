#include "gtest/gtest.h"
#include <iostream>
#include "MedianHeap.h"
using namespace std;
int main(int argc, char **argv) {
        /* If you want to execute random things here in main function just make sure
           lines 10 and 11 are commented.
           If you want to run all the test cases lines 10 and 11 should be the ONLY UNCOMMENTED lines
        */
        ::testing::InitGoogleTest(&argc, argv);
        return RUN_ALL_TESTS();
}

